# Web Framework
Flask
Flask-SQLAlchemy
Flask-Migrate
psycopg2-binary   # Required if you use PostgreSQL on your host
gunicorn          # Required for almost all production hosting (Heroku/Render/etc)

# Environment Variables
python-dotenv

# Google Gemini API
google-generativeai

# Local AI / Vector Math
# This single line will auto-install torch, numpy, transformers, scikit-learn, etc.
sentence-transformers